spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${SPRING_BOO_HW}
    username: admin
    password: admin
  cache:
    redis:
      time-to-live: 7
  data:
    redis:
      port: 6379
clevertec:
  exceptions:
    handler:
      enable: false